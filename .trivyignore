# Trivy ignore file — CVEs in openclaw's bundled dependencies that cannot be
# patched here. These will be resolved when openclaw upstream releases a new
# version with updated dependencies.

# CVE-2026-29087: @hono/node-server 1.19.9 — authorization bypass via encoded
# slashes in Serve Static Middleware. Fixed in 1.19.10, but the version is
# pinned inside openclaw's own node_modules and is not upgradeable from here.
# Risk is low for this deployment: the static middleware is not exposed
# externally (no Ingress/Route; access only via kubectl port-forward).
CVE-2026-29087
